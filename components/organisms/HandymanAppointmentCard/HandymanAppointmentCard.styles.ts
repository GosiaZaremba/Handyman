import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  topContainer: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    margin: 10,
  },
  rightContainer: {
    flex: 1,
    height: 110,
    paddingLeft: 15,
    justifyContent: 'space-between',
  },
});
