import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  backgroundImage: {
    width: '100%',
    height: '30%',
    justifyContent: 'center',
    alignItems: 'center',
  },
  photoContainer: {
    marginTop: 15,
  },
});
