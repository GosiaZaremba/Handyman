import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    width: '100%',
    marginTop: 20,
  },
  labelContainer: {
    alignSelf: 'flex-start',
    marginBottom: 10,
  },
});
