import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  background: {
    flex: 1,
    paddingVertical: 10,
  },
  header: {
    justifyContent: 'center',
    alignItems: 'center',
  },
});
